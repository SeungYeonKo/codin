using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Example04 : MonoBehaviour
{
    // int height = 162;
    //string name = "고승연";
    //멤버 변수(프로퍼티)
    //클래스 내부, 함수 외부에서 선언되는 변수
    //해당 클래스 내에서 접근 및 사용이 가능

   // int age = 23;

    void Start()
    {
        //int age = 20;
        //Debug.Log(age);

        //Debug.Log(name);

        //float height = 188;
        //Debug.Log(height);
        //int age = 222;
        //age = 999;
        //Debug.Log(age);
    }

    void Update()
    {
       // Debug.Log(name); 
        //Debug.Log(height);
    }
}

/*
지역변수 : 함수 내부에서 선언되는 변수    
함수 내부에서 선언된 변수(지역변수)는  내부에서만 사용이 가능
 */